# Core runtime dependencies (pinned to compatible lower bounds; see pyproject for extras).
# The core pipeline runs with only these. GRIB / boosting / SHAP are optional extras.
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
scipy>=1.11
pydantic>=2.5
PyYAML>=6.0
requests>=2.31
joblib>=1.3
matplotlib>=3.7

# Optional (install manually if you want these features):
#   xgboost>=2.0          # residual gradient boosting (falls back to sklearn HistGBR if absent)
#   lightgbm>=4.0         # alternative boosting backend
#   xarray>=2024.1        # GRIB forecast archives
#   cfgrib>=0.9.10        # requires system ecCodes library
#   eccodes>=1.6
#   herbie-data>=2024.1   # HRRR/GFS/GEFS/NBM archive discovery on AWS Open Data
#   fsspec>=2024.1
#   s3fs>=2024.1
#   shap>=0.44            # SHAP explanations for tree models
